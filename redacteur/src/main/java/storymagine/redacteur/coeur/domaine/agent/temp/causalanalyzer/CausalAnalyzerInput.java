package storymagine.redacteur.coeur.domaine.agent.temp.causalanalyzer;

/** Input for CausalAnalyzer â€” concatenated chapter plans for the whole book. */
public record CausalAnalyzerInput(String plansText) {}
