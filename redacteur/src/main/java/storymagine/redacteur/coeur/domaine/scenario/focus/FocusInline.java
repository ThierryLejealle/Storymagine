package storymagine.redacteur.coeur.domaine.scenario.focus;

/** Inline focus text written directly in the chapter — not from the pool. */
public record FocusInline(String text) implements FocusItem {}
