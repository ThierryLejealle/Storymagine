package storymagine.redacteur.coeur.domaine.agent.global.characterchecker;

/** Input for CharacterChecker — full chapter text + GÉNÉRAL sections of character sheets. */
public record CharacterCheckerInput(String text, String charactersText) {}
