package storymagine.redacteur.coeur.domaine.agent.sequence.naturalitycorrector;

/** Input for NaturalityCorrector — the full chapter text to analyse. */
public record NaturalityCorrectorInput(String text) {}
