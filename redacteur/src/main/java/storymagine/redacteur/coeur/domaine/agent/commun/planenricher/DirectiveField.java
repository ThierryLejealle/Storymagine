package storymagine.redacteur.coeur.domaine.agent.commun.planenricher;

/** Fields available in a SequenceDirective that can be injected into a plan JSON. */
public enum DirectiveField {
    CHECKS, FOCUS, LORE, CONSTRAINTS
}
